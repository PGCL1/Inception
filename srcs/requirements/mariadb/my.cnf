[mysqld]

user = mysql
datadir = /var/lib/mysql
socket = /run/mysqld/mysqld.sock
pid-file = /run/mysqld/mysqld.pid

# Listen on localhost TCP
port = 3306
bind-address = 0.0.0.0
log-error = /var/log/mysql/error.log
skip-networking = 0
